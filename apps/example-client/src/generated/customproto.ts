// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v7.34.1
// source: customproto.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "networktables";

/** This message represents an example sensor or application data that you might need to share. */
export interface TestData {
  /** A timestamp for data synchronization */
  timestamp: number;
  /** The main sensor reading */
  value: number;
  /** Optional textual metadata or description */
  info: string;
}

/** This message represents a collection or array of sensor readings. */
export interface TestDataArray {
  /** A list of sensor data readings */
  data: TestData[];
}

function createBaseTestData(): TestData {
  return { timestamp: 0, value: 0, info: "" };
}

export const TestData: MessageFns<TestData> = {
  encode(message: TestData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== 0) {
      writer.uint32(8).uint64(message.timestamp);
    }
    if (message.value !== 0) {
      writer.uint32(17).double(message.value);
    }
    if (message.info !== "") {
      writer.uint32(26).string(message.info);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TestData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestamp = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.value = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.info = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TestData {
    return {
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
      info: isSet(object.info) ? globalThis.String(object.info) : "",
    };
  },

  toJSON(message: TestData): unknown {
    const obj: any = {};
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.value !== 0) {
      obj.value = message.value;
    }
    if (message.info !== "") {
      obj.info = message.info;
    }
    return obj;
  },

  create(base?: DeepPartial<TestData>): TestData {
    return TestData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TestData>): TestData {
    const message = createBaseTestData();
    message.timestamp = object.timestamp ?? 0;
    message.value = object.value ?? 0;
    message.info = object.info ?? "";
    return message;
  },
};

function createBaseTestDataArray(): TestDataArray {
  return { data: [] };
}

export const TestDataArray: MessageFns<TestDataArray> = {
  encode(message: TestDataArray, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      TestData.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TestDataArray {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestDataArray();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(TestData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TestDataArray {
    return { data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => TestData.fromJSON(e)) : [] };
  },

  toJSON(message: TestDataArray): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => TestData.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<TestDataArray>): TestDataArray {
    return TestDataArray.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TestDataArray>): TestDataArray {
    const message = createBaseTestDataArray();
    message.data = object.data?.map((e) => TestData.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
